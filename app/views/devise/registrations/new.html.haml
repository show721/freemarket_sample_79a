.signup-wrapper
  %main.single-main
    %section.single-container
      %h2.single-head__registration 会員情報入力
      .single-head__registration__form
        .single-content
          = form_for(resource, as: resource_name, url: registration_path(resource_name)) do |f|
            = render "devise/shared/error_messages", resource: resource
            .single-content__nickname
              = f.label :ニックネーム
              %span.single-content__require 必須
              %br/
              = f.text_field :nickname, autofocus: true, autocomplete: "nickname", placeholder: "例)テク太テク男"
            .single-content__email
              = f.label :メールアドレス
              %span.single-content__require 必須
              %br/
              = f.email_field :email, autofocus: true, autocomplete: "email", placeholder: "PC・携帯どちらでも可"
              .single-content__password
                = f.label :パスワード
                %span.single-content__require 必須
                %br/
                = f.password_field :password, autocomplete: "new-password", placeholder: "7文字以上"
              .single-content__password_confirmation
                = f.label :パスワード確認
                %span.single-content__require 必須
                %br/
                = f.password_field :password_confirmation, autocomplete: "new-password", placeholder:"7文字以上"
              .single-sub-head
                %h3.single-sub-head__left-text 本人確認
                %p.single-sub-head__text
                  安心・安全にご利用いただくために、お客さまの本人情報の登録にご協力ください。他のお客さまに公開されることはありません。
            .signup-main__form__name
              %p
                お名前(全角)
              %span.slingle-content__require 必須
            .signup-main__form__name__inputbox
              = f.text_field :last_name_kanji, autocomplete: "last_name_kanji", placeholder: '例) 山田', class: "signup-main__form__name__inputbox__last-name-input"
              = f.text_field :first_name_kanji, autocomplete: "first_name_kanji", placeholder: '例) 彩', class: "signup-main__form__name__inputbox__first-name-input"
            .signup-main__form__name-kana
            .single-content__user-full-name-kana
              = f.label :"お名前カナ(全角)"
              %span.single-content__require 必須
              %br/
              = f.text_field :last_name_kana, autocomplete: "last_name_kana", placeholder: "例)ヤマダ", class: "signup-main__form__name__inputbox__last-name-input"
              = f.text_field :first_name_kana, autocomplete: "first_name_kana", placeholder: "例)アヤ", class: "signup-main__form__name__inputbox__first-name-input"
            .single-content__user__birthday
              = f.label :"生年月日"
              %span.slingle-content__require 必須
              %br/
              .birthday-select-wrap
                != sprintf(f.date_select(:birthday, start_year: (Time.now.year), end_year: (Time.now.year - 119), default: Date.new((Time.now.year)), prompt: "--", use_month_numbers: true, date_separator:'%s'),'年 ','月 ')+'日'
              %p.form-into-text ※ 本人情報は正しく入力してください。会員登録後、修正するにはお時間を頂く場合があります。
            .form-text-center-on
              %p.form-text-center 「次へ進む」のボタンを押すことにより、利用規約に同意したものとみなします。
              %br/
              = f.submit "次へ進む", class: 'next-btn'
            .single-content-text__right
              本人情報の登録について
  = render "devise/shared/links"
