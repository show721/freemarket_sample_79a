- if current_user.already_liked?(product) 
  = link_to product_likes_path(product), class:'like-Btn', method: :delete, remote: true do
    %i.fa.fa-heart いいねを取り消す
- else
  = link_to product_likes_path(product), class:'like-Btn', method: :post, remote: true do
    %i.fa.fa-heart いいね！